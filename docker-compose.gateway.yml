services:
  gateway:
    build:
      context: ./gateway
    ports:
      - "80:80"
      - "443:443"
    restart: always
    volumes:
      - ./gateway/nginx.prod.conf:/etc/nginx/nginx.conf
      - /etc/nginx/ssl:/etc/nginx/ssl
