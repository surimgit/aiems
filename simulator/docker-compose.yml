services:
  mqtt-broker:
    image: eclipse-mosquitto:2.0
    container_name: mqtt-broker
    ports:
      - "1883:1883"
    command: >
      sh -c "echo 'listener 1883' > /mosquitto/config/mosquitto.conf &&
             echo 'allow_anonymous true' >> /mosquitto/config/mosquitto.conf &&
             /usr/sbin/mosquitto -c /mosquitto/config/mosquitto.conf"
    healthcheck:
      test: ["CMD", "mosquitto_sub", "-t", "$$SYS/#", "-C", "1", "-h", "localhost"]
      interval: 2s
      timeout: 5s
      retries: 10

  mqtt-logger:
    image: eclipse-mosquitto:2.0
    container_name: mqtt-logger
    restart: always
    depends_on:
      mqtt-broker:
        condition: service_healthy
    command: mosquitto_sub -h mqtt-broker -t "#" -v

  ess-simulator:
    build:
      context: ./ess-simulator
      dockerfile: Dockerfile
    container_name: ess-simulator
    restart: always
    command: ["python", "main.py", "--no-cli", "--config", "/app/config/devices.docker.yaml"]
    depends_on:
      mqtt-broker:
        condition: service_healthy

volumes:
  mosquitto-data:
  mosquitto-log:
